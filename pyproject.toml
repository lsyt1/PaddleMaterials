[build-system]
requires = [
    "setuptools==68.2.2",
    "setuptools-scm==8.3.1",
    "wheel",
    "Cython==3.0.12",
    "numpy==1.26.4",
    "packaging==25.0",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_file = "ppmat/_version.py"
